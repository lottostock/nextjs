import * as firebase from "@firebase/testing";
interface AuthInfo {
    uid: string;
    email?: string;
}
interface IFirestoreRuleTestConfig {
    projectId: string;
}
export declare class FirestoreRuleTest {
    private static readonly EMULATOR;
    readonly projectId: string;
    readonly firestore: firebase.firestore.Firestore;
    readonly admin: firebase.firestore.Firestore;
    private readonly auth;
    constructor(auth?: AuthInfo, config?: IFirestoreRuleTestConfig);
    constructor(auth?: string, config?: IFirestoreRuleTestConfig);
    loadRules(pathToRules: string): Promise<void>;
    clearFirestoreData(): Promise<void>;
    static deleteApps(): Promise<any[]>;
    static start(): Promise<void>;
    static stop(): Promise<void>;
    private static wait;
}
export {};
//# sourceMappingURL=firestore_rule_test.d.ts.map