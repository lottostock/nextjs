/// <reference types="node" />
import { StorageEventType } from "./firestore_storage";
import { EventEmitter } from "events";
export declare type TrackedEventMap = {
    [key in StorageEventType]?: {
        [collection: string]: number;
    };
};
export declare class StorageEventAccumulator {
    private readonly emitter;
    private events;
    private readonly trackerFunc;
    constructor(emitter: EventEmitter);
    private track;
    off(): void;
    reset(): void;
    get(): TrackedEventMap;
}
//# sourceMappingURL=accumulator.d.ts.map